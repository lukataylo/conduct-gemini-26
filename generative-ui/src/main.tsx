import React from "react";
import ReactDOM from "react-dom/client";
import ApertureApp from "./aperture/App";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ApertureApp />
  </React.StrictMode>
);
